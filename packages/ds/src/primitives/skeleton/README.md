# Skeleton

Shimmer placeholders for loading states. Import the keyframes once: `import "@ds/primitives/skeleton/skeleton.css"`.

```tsx
<Skeleton variant="avatar" />
<Skeleton lines={3} />
<Skeleton variant="card" />
```
