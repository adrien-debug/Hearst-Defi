# Calendar

Headless month calendar with full keyboard navigation (arrows, PgUp/PgDn, Home/End, Enter/Space).

```tsx
<Calendar value={date} onChange={setDate} minDate={today} />
<Calendar range value={{ from, to }} onChange={setRange} />
<Calendar multi value={dates} onChange={setDates} />
```
