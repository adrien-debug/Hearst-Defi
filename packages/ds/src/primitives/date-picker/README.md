# DatePicker

Input + popover wrapping `<Calendar>`. Closes on outside click / Escape.

```tsx
<DatePicker value={date} onChange={setDate} placeholder="Settlement" />
<DatePicker range value={range} onChange={setRange} />
```
