// Breadcrumb composes raw utility strings inline because the four sub-parts
// share no common variants. This file exists for naming-symmetry only.
export const breadcrumbVariantNames = ["default"] as const;
