/**
 * Typed re-export of `class-variance-authority`.
 *
 * Prefer `tv()` from `./tv` for new components (better compound variant DX).
 * `cva` remains the documented fallback per CONTRACT.md §3.4.
 */
export { cva, cx } from "class-variance-authority";
export type { VariantProps } from "class-variance-authority";
