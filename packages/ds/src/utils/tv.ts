/**
 * Typed re-export of `tailwind-variants`.
 *
 * Preferred over `cva` for new primitives (compound variants, slots,
 * extension via `extend`, type-inferred `VariantProps`).
 */
export { tv } from "tailwind-variants";
export type { VariantProps as TVVariantProps } from "tailwind-variants";
